import { ListActiveDirective } from './list-active.directive';

describe('ListActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ListActiveDirective();
    expect(directive).toBeTruthy();
  });
});
